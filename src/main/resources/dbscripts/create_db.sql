CREATE TABLE product (
  id int,
  name varchar(100),
  description varchar(200),
  price float,
  created_date date
 );

 CREATE TABLE user (
   id int,
   username varchar(100),
   password varchar(20),
 );