<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.ohapon.eshop"/>

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location" value="classpath:/application.properties"/>
    </bean>

    <bean id="dataSource" class="org.h2.jdbcx.JdbcDataSource">
        <property name="url" value="${db.url}"/>
        <property name="user" value="${db.user}"/>
        <property name="password" value="${db.password}"/>
    </bean>

    <bean id="viewResolver" class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
        <property name="cache" value="true"/>
        <property name="prefix" value=""/>
        <property name="suffix" value=".html"/>
        <property name="contentType" value="text/html;charset=UTF-8"/>
    </bean>

    <bean id="freemarkerConfig" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <property name="templateLoaderPath" value="classpath:/templates"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/product/*"/>
            <mvc:mapping path="/cart/*"/>
            <mvc:mapping path="/cart"/>
            <bean class="plazma.ups.eshop.web.security.SecurityInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!--
    <bean id="dataSource" class="org.hsqldb.jdbc.JDBCDataSource">
        <property name="database" value="${db.url}"/>
        <property name="user" value="${db.user}"/>
        <property name="password" value="${db.password}"/>
    </bean>
    -->

    <!--
    <bean id="productService" class="plazma.ups.eshop.service.ProductService">
        <property name="productDao" ref="jdbcProductDao"/>
    </bean>

    <bean id="userService" class="plazma.ups.eshop.service.UserService">
        <property name="userDao" ref="jdbcUserDao"/>
    </bean>

    <bean id="securityService" class="plazma.ups.eshop.service.SecurityService">
        <property name="userService" ref="userService"/>
    </bean>

    <bean id="jdbcProductDao" class="plazma.ups.eshop.dao.jdbc.JdbcProductDao">
        <property name="jdbcTemplate" ref="jdbcTemplate"/>
    </bean>

    <bean id="jdbcUserDao" class="plazma.ups.eshop.dao.jdbc.JdbcUserDao">
        <property name="jdbcTemplate" ref="jdbcTemplate"/>
    </bean>
    -->

    <mvc:annotation-driven/>

</beans>